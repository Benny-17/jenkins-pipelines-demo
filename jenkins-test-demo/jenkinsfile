pipeline {
  agent any

  stages {
    stage('Install Dependencies') {
      steps {
        dir('jenkins-test-demo') {
          sh 'npm install'
        }
      }
    }

    stage('Run Tests') {
      steps {
        dir('jenkins-test-demo') {
          sh 'npm test'
        }
      }
    }
  }
}
